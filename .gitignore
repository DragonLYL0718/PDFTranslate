# --- Node / frontend ---
node_modules
dist
dist-desktop
dist-ssr
*.local
.vite
npm-debug.log*
yarn-error.log*

# --- Tauri desktop shell ---
src-tauri/target/
src-tauri/gen/
# Vendored by scripts/fetch-uv.mjs and scripts/stage-backend.mjs at build time.
src-tauri/binaries/
src-tauri/resources/

# TypeScript incremental build cache
*.tsbuildinfo

# --- Python / backend ---
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Secrets ---
.env
.env.*
!.env.example
# Not a secret: selects the desktop build target, and the build breaks without it.
!.env.desktop

# --- Local test artifacts (sample PDFs dropped in the repo root) ---
/*.pdf

# --- Editor / OS ---
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea
.claude/settings.local.json

CLAUDE.md

.codebuddy/