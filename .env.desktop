# Loaded by `vite --mode desktop`. Read as import.meta.env.VITE_TARGET, which
# lets the platform adapter pick its implementation at build time so the unused
# one tree-shakes away (see src/platform/index.ts).
VITE_TARGET=desktop
